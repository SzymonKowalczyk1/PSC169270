#include <stdio.h>
#include <stdlib.h>

double fun(double(*w)(int),int n)
{
return fun(a);
}
double pom(int arg)
{
    return (double)(arg+3);
}

int main()
{
    printf("%lf\n",foo(pom, 5));
    return 0;
}
